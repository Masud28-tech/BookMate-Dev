spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookmate
    username: bookmate
    password: bookmate
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database:postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 1025
    username: "masood"
    password: "password"
    properties:
      mail:
        smtp:
          trust: '*'
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

application:
  jwt:
    secret-key: 7b2e8c4e-2f7a-4c8e-9b1a-8e2d4f6c9a1b
    expiration: 86400000 # 1 day in milliseconds
